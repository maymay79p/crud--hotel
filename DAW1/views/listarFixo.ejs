<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Lista de Serviços Fixos</title>
    <style>
        body { 
            background-color: #f6eee3; 
            color: #5a3e36; 
            font-family: 'Segoe UI', sans-serif; 
        }
        .container { 
            width: 90%;
            margin: 40px auto;
            background-color: #fffaf5;
            padding: 25px;
            border-radius: 14px;
            box-shadow: 0 8px 20px rgba(90, 62, 54, 0.3);
            border: 2px solid #a0522d;
        }
        
        h1 { text-align: center; color: #8b4513; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; background-color: #fffdfb; }
        th, td { border: 2px solid #a0522d; padding: 12px 15px; text-align: center; }
        th { background-color: #cd853f; color: white; font-weight: bold; }
        tr:nth-child(even) { background-color: #fef7f1; }
        tr:hover { background-color: #f5e5d8; }
        
         .add-link {
            display: inline-block;
            margin-bottom: 10px;
            background-color: #cd853f;
            color: white;
            padding: 8px 12px;
            border-radius: 8px;
            text-decoration: none;
        }

        .add-link:hover {
            background-color: #a0522d;
        }
         .btn {
            background-color: #cd853f;
            color: white;
            border: none;
            padding: 6px 10px;
            border-radius: 6px;
            cursor: pointer;
            margin: 2px;
        }
        .btn:hover {
            background-color: #a0522d;
        }
        .form-filtro {
            margin-bottom: 15px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Lista de Serviços Fixos</h1>
<a class="add-link" href="/fixo/add">➕ Novo Fixo</a>

        <form form class="form-filtro" action="/fixo/lst" method="POST">
            <input type="text" name="filtro" placeholder="Buscar...">
            <button class="btn" type="submit">Buscar</button>
            
        </form>

        <table>
            <tr>
                <th>Quarto</th>
                <th>Serviço</th>
                <th>Descrição</th>
                <th>Ações</th>
            </tr>
            <% fixos.forEach(fixo => { %>
                <tr>
                    <td>
                        N°: <%= fixo.quarto.numero %> <br>
                        Andar: <%= fixo.quarto.andar %>
                    </td>
                    <td><%= fixo.servico.descricao %></td>
                    <td><%= fixo.Descricao %></td>
                    <td>
                        <a class="btn" href="/fixo/edit/<%= fixo._id %>">Editar</a>
                        <form action="/fixo/delete/<%= fixo._id %>" method="POST">
                            <button class="btn" type="submit">Excluir</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </table>
    </div>
</body>
</html>
