<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    
    <title>Lista de Tipos de Quarto</title>
    <style>
        body {
            background-color: #fdf6f0;
            color: #5a3e36;
            font-family: Arial, sans-serif;
        }
        .container {
            width: 90%;
            max-width: 1000px;
            margin: 30px auto;
            background-color: #fff7f0;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(90, 62, 54, 0.3);
        }
        h1 {
            text-align: center;
            color: #a0522d;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        th, td {
            border: 1px solid #a0522d;
            padding: 10px;
            text-align: center;
        }
        th {
            background-color: #cd853f;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #fffaf5;
        }
        .btn {
            background-color: #cd853f;
            color: white;
            border: none;
            padding: 6px 10px;
            border-radius: 6px;
            cursor: pointer;
            margin: 2px;
        }
        .btn:hover {
            background-color: #a0522d;
        }
        .form-filtro {
            margin-bottom: 15px;
            text-align: center;
        }
        input[type="text"] {
            padding: 6px;
            border-radius: 6px;
            border: 1px solid #a0522d;
            width: 200px;
            background-color: #fffaf5;
        }
        .add-link {
            display: inline-block;
            margin-bottom: 10px;
            background-color: #cd853f;
            color: white;
            padding: 8px 12px;
            border-radius: 8px;
            text-decoration: none;
        }
        .add-link:hover {
            background-color: #a0522d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Lista de Tipos de Quarto</h1>

        <a class="add-link" href="/tipquarto/add">➕ Adicionar Tipo de Quarto</a>

        <form class="form-filtro" action="/tipquarto/lst" method="POST">
            <input type="text" name="filtro" placeholder="Buscar por nome">
            <button class="btn" type="submit">Buscar</button>
        </form>

        <table>
            <tr>
                <th>Nome</th>
                <th>Descrição</th>
                <th>Ações</th>
            </tr>
            <% tipquartos.forEach(tipquarto => { %>
                <tr>
                    <td><%= tipquarto.nome %></td>
                    <td><%= tipquarto.descricao %></td>
                    <td>
                        <a class="btn" href="/tipquarto/edit/<%= tipquarto._id %>">Editar</a>
                        <form style="display:inline;" action="/tipquarto/delete/<%= tipquarto._id %>" method="POST">
                            <button class="btn" type="submit">Excluir</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </table>
    </div>
</body>
</html>
