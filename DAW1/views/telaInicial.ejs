//é só a base//*******

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hotel Alpinia Palace</title>

    <style>
        /* 🎨 Paleta Outono */
        :root {
            --marrom-escuro: #4B2E2A;
            --marrom-claro: #A05A2C;
            --laranja-outono: #C97C48;
            --dourado-folha: #E8C48C;
            --bege-fundo: #FAF4EF;
            --verde-oliva: #8A8D5A;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bege-fundo);
            font-family: 'Georgia', serif;
            color: var(--marrom-escuro);
        }

        /* 🌄 Cabeçalho */
        header {
            background: linear-gradient(rgba(75,46,42,0.7), rgba(75,46,42,0.7)),
                        url('https://images.unsplash.com/photo-1506744038136-46273834b3fb') no-repeat center/cover;
            padding: 80px 20px;
            text-align: center;
        }

        .logo h1 {
            color: var(--dourado-folha);
            font-size: 64px;
            text-shadow: 2px 2px var(--marrom-escuro);
            letter-spacing: 2px;
        }

        nav {
            margin-top: 30px;
        }

        nav ul {
            list-style: none;
        }

        nav ul li {
            display: inline-block;
            margin: 0 20px;
        }

        nav ul li a {
            text-decoration: none;
            background-color: var(--marrom-claro);
            color: var(--dourado-folha);
            padding: 10px 25px;
            border-radius: 30px;
            transition: background-color 0.3s;
            font-weight: bold;
        }

        nav ul li a:hover {
            background-color: var(--marrom-escuro);
        }

        /* 🏞️ Apresentação */
        main {
            display: flex;
            justify-content: center;
            padding: 70px 20px;
        }

        .apresentacao {
            background-color: white;
            border: 3px solid var(--dourado-folha);
            padding: 50px;
            max-width: 900px;
            border-radius: 25px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .apresentacao p {
            font-size: 18px;
            line-height: 1.8;
            color: var(--marrom-escuro);
            margin-bottom: 40px;
        }

        /* 🔘 Botão */
        .apresentacao button {
            background-color: var(--laranja-outono);
            color: white;
            border: none;
            padding: 15px 45px;
            border-radius: 50px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }

        .apresentacao button:hover {
            background-color: var(--marrom-claro);
            transform: scale(1.05);
        }

        /* 🍂 Rodapé */
        footer {
            background-color: var(--marrom-escuro);
            color: var(--dourado-folha);
            text-align: center;
            padding: 20px;
        }

        footer p {
            font-size: 14px;
        }

    </style>
</head>

<body>

    <header>
        <div class="logo">
            <h1>Hotel Alpinia Palace</h1>
        </div>
        <nav>
            <ul>
                <li><a href="#">Instalações</a></li>
                <li><a href="#">Restaurantes</a></li>
                <li><a href="#">Atividades</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section class="apresentacao">
            <p>
                Bem-vindo ao <strong>Hotel Alpinia Palace</strong>, um refúgio elegante em meio às cores do outono.
                Desfrute de acomodações aconchegantes, gastronomia sofisticada e paisagens deslumbrantes.
                Viva experiências únicas, rodeado pela natureza, pelo charme das folhas douradas e pela tranquilidade
                das montanhas.
            </p>
            <button>Reserve Agora</button>
        </section>
    </main>

    <footer>
        <p>© 2025 Hotel Alpinia Palace | Todos os direitos reservados</p>
    </footer>

</body>
</html>
