<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Confirmação de Reserva</title>
    <style>
        body {
            background-color: #fef7f5;
            font-family: 'Georgia', serif;
            color: #2e2e2e;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 90%;
            max-width: 800px;
            margin: 60px auto;
            background-color: #fff;
            border: 2px solid #d48c8c;
            border-radius: 20px;
            padding: 60px 40px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            position: relative;
        }

        h1 {
            text-align: center;
            font-size: 38px;
            margin-bottom: 20px;
        }

        .nome {
            text-align: center;
            font-family: 'Cursive', serif;
            font-size: 32px;
            color: #8b1e1e;
            margin-bottom: 40px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
        }

        .assinatura {
            text-align: center;
            font-family: 'Georgia', serif;
            margin-top: 60px;
        }

        .assinatura span {
            font-family: 'Cursive', serif;
            font-style: italic;
            font-size: 42px;
        }

    
        .flor {
            position: absolute;
            width: 80px;
            opacity: 0.4;
        }

        .flor.tl {
            top: 10px;
            left: 10px;
        }
        .flor.tr {
            top: 10px;
            right: 10px;
            transform: scaleX(-1);
        }
        .flor.bl {
            bottom: 10px;
            left: 10px;
            transform: scaleY(-1);
        }
        .flor.br {
            bottom: 10px;
            right: 10px;
            transform: scale(-1, -1);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Esperamos por você</h1>
        <div class="nome">
            <%= cliente.nome %>!
        </div>

        <div class="assinatura">
            <p>Equipe <span>Aphínia</span> Palace</p>
        </div>
    </div>
</body>
</html>
